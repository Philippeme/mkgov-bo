{# templates/admin/permission/new.html.twig #}
{% extends 'admin/base.html.twig' %}

{% block title %}Create New Permission{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1><i class="fas fa-key me-2"></i>Create New Permission</h1>
        <p class="text-muted mb-0">Add a new permission to the system</p>
    </div>
    <div>
        <a href="{{ path('admin_permission_index') }}" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-1"></i> Back to Permissions
        </a>
    </div>
</div>

{{ include('admin/permission/_form.html.twig') }}
{% endblock %}